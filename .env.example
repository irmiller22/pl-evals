# make serve loads .env if present. Never commit real credentials.
# Set an exact Anthropic model ID; an optional anthropic/ prefix is accepted.
APP_MODEL=
# APP_MODEL falls back to BASELINE_MODEL when empty.
BASELINE_MODEL=
CANDIDATE_MODEL=
JUDGE_MODEL=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
